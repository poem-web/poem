[package]
name = "example-unix-socket"
version = "0.1.0"
edition = "2018"

[dependencies]
poem = { path = "../../", features = ["tracing"] }
tokio = { version = "1.10.0", features = ["rt-multi-thread", "macros"] }
